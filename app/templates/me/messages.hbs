<h1>Messages for <span class="d-inline-block">{{this.session.user.callsign}}</span></h1>
<BackToHome/>

<p>
  Clubhouse Messages are one way for various Ranger departments to communicate with Rangers
  while the event is happening.
</p>

{{#if this.shouldUseContactRanger}}
  <p>
    Use the <LinkTo @route="me.contact">Me &gt; Contact GPE member</LinkTo> feature to send a message to another
    staff member or volunteer of GPE.
  </p>
{{/if}}

<ClubhouseMessages @person={{this.session.user}} @messages={{this.messages}} @isMe={{true}} />
